 # 内容目录（可选）。不设置时按以下顺序自动查找：
 # 1. 本环境变量指向的目录
 # 2. 项目内 bughub-content/content（git submodule，推荐）
 # 3. 项目内 content/
 # 部署到服务器时，建议指向一个绝对路径，比如把内容仓库单独 clone 出来：
 # BUGHUB_CONTENT_DIR=/data/bughub-content/content
 
